---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="About Trek4Free — Free, Community‑Powered Trail & Camping Maps">
  <Fragment slot="head">
    <meta name="description" content="Trek4Free is a free, community‑powered map for hiking trails, trailheads, waterfalls, swimming holes, and free campsites. No logins. No paywalls. Just trails." />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet" />
  </Fragment>

  <main class="aboutfresh">
    <!-- HERO -->
    <section class="hero">
      <div class="hero__bg"></div>
      <div class="hero__shade"></div>
      <div class="hero__inner">
        <h1 class="title">Built by hikers. Shared by everyone.</h1>
        <p class="tag">
          Your free, community‑powered guide to <strong>trails</strong>, <strong>trailheads</strong>,
          <strong>waterfalls & swimming holes</strong>, and <strong>no‑fee campsites</strong>.
          No logins. No paywalls. Just the info you need to go.
        </p>
        <div class="btnrow">
          <a class="btn btn--primary" href="/explore">Explore the Map</a>
          <a class="btn btn--outline" href="/trail-notes">Read Trail Notes</a>
        </div>
      </div>
    </section>

    <!-- GOLDEN ICONS BAND (centered, no crop) -->
    <section class="section-pad section--dark">
      <div class="divider-container">
  <img src="/images/hero/bear-tracks.png" alt="Bear Tracks Divider" class="bear-divider">
</div>

    </section>

    <!-- DIFFERENTIATORS -->
    <section class="pillars">
      <div class="wrap">
        <h2 class="section">What makes Trek4Free different?</h2>
        <div class="grid">
          <article class="card">
            <h3>Free by design</h3>
            <p>Dispersed camping and trail access with <em>no subscriptions</em>, <em>no logins</em>, and <em>no upsells</em>.</p>
          </article>
          <article class="card">
            <h3>Real access notes</h3>
            <p>Road conditions, fees, seasonality, water & restrooms — the practical details that actually matter.</p>
          </article>
          <article class="card">
            <h3>Community‑powered</h3>
            <p>Public map sources + on‑the‑ground reports. Share what you know and help the next person adventure smarter.</p>
          </article>
          <article class="card">
            <h3>Print‑ready details</h3>
            <p>Clean pages with coords, topo context, quick links, and a tidy print/PDF layout for the field.</p>
          </article>
        </div>
      </div>
    </section>

    <!-- COMPASS DIVIDER WITH ORANGE LINE -->
        {/*
      If you prefer the tribal footprints instead of the compass row,
      swap the block above for this:
      <section class="divider"><div class="tribal-footprints subtle" aria-hidden="true"></div></section>
    */}

    <!-- HOW IT WORKS -->
    <section class="how">
      <div class="wrap">
        <h2 class="section">How it works</h2>
        <div class="steps">
          <figure class="step">
            <img src="/images/hero/step-one.png" alt="Search the map" />
            <figcaption><strong>1) Search the map.</strong> Filter for hiking, free camping, waterfalls, swimming holes, and more.</figcaption>
          </figure>
          <figure class="step">
            <img src="/images/hero/step-two.png" alt="Open a place" />
            <figcaption><strong>2) Open a place.</strong> Get coords, elevation, sunrise/sunset, access notes, and topo context.</figcaption>
          </figure>
          <figure class="step">
            <img src="/images/hero/step-three.png" alt="Plan and print" />
            <figcaption><strong>3) Plan & print.</strong> Copy coords, open driving directions, or print a clean PDF for the field.</figcaption>
          </figure>
        </div>

        <div class="btnrow btnrow--left">
          <a class="btn btn--primary" href="/explore">Start Planning Free</a>
          <a class="btn btn--outline" href="/search">Search</a>
        </div>
      </div>
    </section>

    <!-- ORIGINS -->
    <section class="origins">
      <div class="texture"></div>
      <div class="wrap origins__grid">
        <div class="origins__text">
          <h2 class="section">Why we built this</h2>
          <p>
            After years of chasing ridge lines, sleeping in trailhead pullouts, and piecing together info across dozens
            of tabs, we wanted a <em>simple, free</em> way to plan hikes and find legal, no‑fee camping — and to give
            that back to the community.
          </p>
          <p class="muted small">
            Want the personal version with dogs, vans, and trail legends? Read the original
            <a href="/about-story">About page</a>.
          </p>
        </div>
        <div class="origins__art">
          <img src="/images/hero/landscape-banner.png" alt="Sunrise over canyon with Topo figure" />
        </div>
      </div>
    </section>

    <!-- CLOSING CTA -->
    <section class="cta">
      <div class="cta__art" aria-hidden="true">
        <img src="/images/hero/start-exploring.png" alt="" />
        <div class="cta__shade"></div>
      </div>
      <div class="cta__inner">
        <h2 class="cta__title">Adventure without friction.</h2>
        <p class="cta__text">Plan smarter. Spend less. Go farther. Trek4Free is your free guide to the good stuff.</p>
        <div class="btnrow">
          <a class="btn btn--primary" href="/explore">Explore the Map</a>
          <a class="btn btn--outline" href="/trail-notes">Read Trail Notes</a>
        </div>
      </div>
    </section>
  </main>

  <style>
    /* ----- Page scope ----- */
    .aboutfresh { background:#0a0a0a; color:#eee; }
    .wrap { max-width:1100px; margin:0 auto; padding:0 1.1rem; }
    .section { font-family:'Bebas Neue',sans-serif; text-transform:uppercase; letter-spacing:.04em; color:#ff7f1a; text-align:center; font-size:clamp(1.6rem,4.6vw,2.2rem); margin:0 0 1.1rem; }
    .muted.small { color:rgba(230,230,230,.72); }

    /* Buttons */
    .btn{ display:inline-flex; align-items:center; justify-content:center; gap:.5rem; padding:.95rem 1.6rem; border-radius:12px; font-weight:800; text-decoration:none; font-size:1rem; transition: transform .15s ease, box-shadow .15s ease, background-color .15s ease, border-color .15s ease; }
    .btn:hover{ transform: translateY(-1px); }
    .btn--primary{ background:#ff7f1a; color:#0b0b0b; border:0; box-shadow:0 10px 28px rgba(255,127,26,.28); }
    .btn--primary:hover{ background:#ff932f; }
    .btn--outline{ background:transparent; color:#ff7f1a; border:2px solid #ff7f1a; }
    .btn--outline:hover{ color:#ff932f; border-color:#ff932f; }
    .btnrow{ display:flex; gap:.9rem; flex-wrap:wrap; justify-content:center; }
    .btnrow--left{ justify-content:flex-start; }

    /* HERO */
    .hero{ position:relative; min-height: clamp(420px, 60vh, 620px); display:grid; place-items:center; padding:4rem 1.1rem 3rem; overflow:hidden; border-bottom:1px solid #111; }
    .hero__bg{ position:absolute; inset:0; background:url('/images/hero/landscape-banner.png') center 52% / cover no-repeat; filter: contrast(1.06) saturate(1.05) brightness(1.02); }
    .hero__shade{ position:absolute; inset:0; background:
      linear-gradient(to bottom, rgba(0,0,0,.55) 0%, rgba(0,0,0,.35) 30%, rgba(0,0,0,.45) 60%, rgba(0,0,0,.78) 100%); }
    .hero__inner{ position:relative; z-index:1; text-align:center; max-width:920px; }
    .title{
      font-family:'Bebas Neue',sans-serif; text-transform:uppercase; letter-spacing:.04em;
      font-size:clamp(2.1rem,6vw,3.6rem); margin:0 0 .6rem;
      background: linear-gradient(45deg, #ffd24d, #cc9900); -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      text-shadow: 0 2px 10px rgba(0,0,0,.6);
    }
    .tag{ color:rgba(255,225,170,.95); font-size:clamp(1.02rem,2.4vw,1.18rem); line-height:1.55; margin:0 0 1.3rem; text-shadow:0 2px 7px rgba(0,0,0,.45); }

    /* ICON BAND (golden-icons) */
    .section-pad { padding: 1.5rem 1rem; }
    .section--dark { background:#101010; }
    .icon-band{
      max-width:1100px;
      height:180px;
      margin:0 auto;
      display:grid;
      place-items:center;
      background:#0f0f0f;
      border-radius:18px;
      box-shadow:0 8px 28px rgba(0,0,0,.25);
      padding:0 16px;
    }
    .icon-band img{
      height:140px; /* keep full art visible; adjust if needed */
      width:auto;
      display:block;
      filter:contrast(1.05) saturate(1.05);
    }

    /* PILLARS */
    .pillars{ background:#0f0f0f; padding:2.4rem 0 2.9rem; }
    .grid{ display:grid; grid-template-columns:repeat(auto-fit,minmax(240px,1fr)); gap:1rem; }
    .card{ background:#171717; border:1px solid #222; border-radius:18px; padding:1rem 1rem 1.1rem; text-align:center; transition:transform .2s ease, box-shadow .22s ease; }
    .card:hover{ transform:translateY(-3px); box-shadow:0 10px 34px rgba(0,0,0,.34); }
    .card h3{ font-family:'Bebas Neue',sans-serif; color:#ff7f1a; font-size:1.26rem; letter-spacing:.03em; margin:.1rem 0 .4rem; }
    .card p{ color:#d9d9d9; margin:0; }

    /* DIVIDERS */
    .divider{ background:#0e0e0e; padding: 0; }

    /* Compass divider with horizontal line */
    .compass-divider{
      --h: 140px;
      height: var(--h);
      background-color:#0e0e0e;
      background-image:url('/images/hero/compas-rows.png'); /* your file name */
      background-repeat: repeat-x;
      background-position:center;
      background-size: auto 100%;
      position: relative;
      filter: contrast(1.05) saturate(1.05);
    }
    .compass-divider::after{
      content:"";
      position:absolute; left:0; top:50%; width:100%; height:2px;
      background: rgba(255,140,0,.65);
      transform: translateY(-50%);
    }

    /* (Optional) Tribal footprints divider if you want to swap later */
    .tribal-footprints{
      --h: 110px;
      height: var(--h);
      background-color:#0e0e0e;
      background-image:url('/images/hero/foot-prints-tile.png');
      background-repeat: repeat-x;
      background-position:center center;
      background-size: auto 100%;
      mask-image: linear-gradient(90deg, transparent 0%, black 8%, black 92%, transparent 100%);
      -webkit-mask-image: linear-gradient(90deg, transparent 0%, black 8%, black 92%, transparent 100%);
    }
    .tribal-footprints.subtle{ opacity:.66; filter:saturate(.95) brightness(.95); }

    /* HOW */
    .how{ background:#0f0f0f; padding:3rem 0 2.4rem; border-top:1px solid #111; border-bottom:1px solid #111; }
    .steps{ display:grid; gap:1rem; grid-template-columns:1fr; }
    @media (min-width: 900px){ .steps{ grid-template-columns: repeat(3, 1fr); } }
    .step{ margin:0; background:#151515; border:1px solid #222; border-radius:18px; overflow:hidden; box-shadow:0 10px 28px rgba(0,0,0,.24); }
    .step img{ width:100%; height:auto; display:block; }
    .step figcaption{ padding:.85rem .9rem; color:#e8e8e8; }

    /* ORIGINS */
    .origins{ position:relative; background:#0f0f0f; padding:3.1rem 0; }
    .texture{ position:absolute; inset:0; background: url('/images/hero/compass-rows.png') center/cover no-repeat; opacity:.14; mix-blend-mode: screen; }
    .origins__grid{ position:relative; z-index:1; display:grid; gap:1.4rem; grid-template-columns:1fr; align-items:center; }
    @media (min-width: 960px){ .origins__grid{ grid-template-columns: 1.1fr .9fr; } }
    .origins__text p{ max-width: 58ch; }
    .origins__art img{ width:100%; height:auto; border-radius:16px; box-shadow:0 12px 36px rgba(0,0,0,.34); }

    /* CTA */
    .cta{ position:relative; overflow:hidden; border-top:1px solid #111; }
    .cta__art{ position:relative; }
    .cta__art img{ width:100%; height:auto; display:block; }
    .cta__shade{ position:absolute; inset:0; background: radial-gradient(40% 40% at 70% 50%, rgba(0,0,0,.35), rgba(0,0,0,.75)); }
    .cta__inner{ position:absolute; inset:0; display:grid; place-items:center; text-align:center; padding:1.8rem 1.1rem; }
    .cta__title{
      font-family:'Bebas Neue',sans-serif; text-transform:uppercase; letter-spacing:.04em;
      font-size:clamp(2rem,5.2vw,3rem); margin:0 0 .5rem;
      background: linear-gradient(45deg, #ffd24d, #cc9900);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      text-shadow: 0 2px 10px rgba(0,0,0,.6);
    }
    .cta__text{ color:#f3e6c9; text-shadow:0 2px 8px rgba(0,0,0,.55); margin:0 0 1rem; max-width: 820px; }

    /* Mobile niceties */
    @media (max-width: 720px){
      .btnrow--left{ justify-content:center; }
    }

.divider-container {
  display: flex;
  justify-content: center;
  margin: 2rem 0; /* space above and below */
}

.bear-divider {
  width: 90%;
  height: auto; /* preserves aspect ratio */
  max-width: 1080px; /* prevents upscaling beyond original size */
}



  </style>
</BaseLayout>
