---
import BaseLayout from "../layouts/BaseLayout.astro";

/** Helper “component” (inline) for photos */
const photos = [
  { src: "/images/aboutus/Amyoverlookingvaley.jpg", alt: "Amy perched above a valley", caption: "Miles to go. Smiles to spare." },
  { src: "/images/aboutus/barrcamp.png", alt: "Barr Camp stop", caption: "Barr Camp cocoa break." },
  { src: "/images/aboutus/Camping.jpg", alt: "Camp scene", caption: "Free campsite, million‑star ceiling." },
  { src: "/images/aboutus/DavidHorton.jpg", alt: "With David Horton", caption: "Trail legends are real people." },
  { src: "/images/aboutus/image (8).jpg", alt: "Trail selfie", caption: "Dusty, hungry, happy." },
  { src: "/images/aboutus/Image4.jpg", alt: "Switchbacks", caption: "Switchbacks and second winds." },
  { src: "/images/aboutus/Image 95 (1).jpg", alt: "Van life", caption: "Home is where the ridge line is." },
  { src: "/images/aboutus/IMG_0032.JPG", alt: "Forest icons", caption: "Pines & topo lines." },
  { src: "/images/aboutus/Image 104.jpg", alt: "Water crossing", caption: "Cold feet, warm hearts." },
  { src: "/images/aboutus/Image 388.jpg", alt: "Trail grin", caption: "Trail grin, day 3." },
  { src: "/images/aboutus/IMG_02419.JPG", alt: "Trail dogs", caption: "Trail dog approved." },
  { src: "/images/aboutus/IMG_02425.JPG", alt: "Trail tape", caption: "Ribbon finds & route memory." },
  { src: "/images/aboutus/IMG_0045.JPG", alt: "Summit snack", caption: "Summit snacks > fancy dinners." },
  { src: "/images/aboutus/IMG_9723.jpg", alt: "Canyon light", caption: "Golden hour chaser." },
  { src: "/images/aboutus/IMG_1255.jpg", alt: "Campsite view", caption: "Waking up to this." },
  { src: "/images/aboutus/IMG_1689.JPG", alt: "Creek time", caption: "Creek cold‑plunge club." },
  { src: "/images/aboutus/IMG_1777.JPG", alt: "Topo tools", caption: "Maps, markers, mission." },
  { src: "/images/aboutus/IMG_1999.jpg", alt: "Trail dog 2", caption: "Four paws, endless stoke." },
  { src: "/images/aboutus/IMG_2060.JPG", alt: "Trail grin 2", caption: "The ‘one more mile’ face." },
  { src: "/images/aboutus/Image 103.jpg", alt: "Snow day rig", caption: "Snow day, go day." },
  { src: "/images/aboutus/IMG_0035.JPG", alt: "Dog nap", caption: "Trail naps hit different." },
  { src: "/images/aboutus/IMG_2680.jpg", alt: "Summit duo", caption: "Up, up, up." },
  { src: "/images/aboutus/IMG_02799.JPG", alt: "The van", caption: "Camp kitchen on wheels." },
  { src: "/images/aboutus/IMG_3274.jpg", alt: "Trail lunch", caption: "Wraps & waterfalls." },
  { src: "/images/aboutus/IMG_3407.jpg", alt: "Campfire", caption: "Spark, stories, stars." },
  { src: "/images/aboutus/IMG_4944.jpg", alt: "Couple at camp", caption: "Best meetings happen outside." },
  { src: "/images/aboutus/IMG_6783.jpg", alt: "Ridge run", caption: "Chasing ridge lines." },
  { src: "/images/aboutus/IMG_0940.jpg", alt: "Camp coffee", caption: "Pour‑over, trail‑side." },
  { src: "/images/aboutus/IMG_8331.jpg", alt: "Happy pup", caption: "Trail dog smile quota met." },
  { src: "/images/aboutus/IMG_6792.jpg", alt: "Splash zone", caption: "Swimming holes forever." },
];
---

<BaseLayout title="About — The Personal Version">
  <Fragment slot="head">
    <meta name="description" content="The personal story behind Trek4Free — a scrapbook of ridge lines, vans, trail dogs, and serendipity." />
  </Fragment>

  <main class="scrapbook">
    <!-- Hero / title -->
    <section class="hero">
      <div class="hero__paper"></div>
      <div class="hero__ink"></div>
      <div class="hero__inner">
        <h1 class="title">The Personal Version</h1>
        <p class="lede">Dogs, vans, camp coffee, trail angels, and the long way around. This is the heart behind Trek4Free.</p>
        <div class="hero__icons">
          <img src="/images/hero/wolf.png" alt="" />
          <img src="/images/hero/eagle.png" alt="" />
          <img src="/images/hero/owl.png" alt="" />
        </div>
      </div>
    </section>

    <!-- Origin story -->
    <section class="story">
      <div class="wrap">
        <div class="paper-card">
          <h2>How it started</h2>
          <p>
            We didn’t set out to build a website. We set out to chase ridge lines, find free campsites, and see how far
            a tank of gas and a good map could take us. Along the way we collected coordinates on napkins, scribbled gate
            notes, and swapped beta at trailheads. Eventually we turned those scattered bits into Trek4Free — so you can
            skip the scavenger hunt and get straight to the good stuff.
          </p>
        </div>
      </div>
    </section>

    <!-- Feature mini sections -->
    <section class="mini">
      <div class="wrap mini__grid">
        <article class="mini__card">
          <h3>The Van</h3>
          <p>Sleeping in pullouts, coffee at sunrise, driving wherever the contour lines get interesting.</p>
        </article>
        <article class="mini__card">
          <h3>Trail Dogs</h3>
          <p>They set the pace, choose the swimming holes, and veto bad decisions. We trust their noses.</p>
        </article>
        <article class="mini__card">
          <h3>Trail Legends</h3>
          <p>From David Horton to the stranger with duct tape—we’ve learned this is a generous community.</p>
        </article>
      </div>
    </section>

    <!-- Masonry photo wall -->
    <section class="gallery">
      <div class="wrap">
        <h2 class="subhead">Snapshots from the road</h2>

        <div class="grid">
          {photos.map((p, i) => (
            <figure class="polaroid" style={`--r:${(i % 5) - 2}deg`}>
              <a href={`#photo-${i}`} class="polaroid__link">
                <img loading="lazy" src={p.src} alt={p.alt} />
              </a>
              <figcaption>{p.caption}</figcaption>
              {/* Lightbox modal target */}
              <div class="lightbox" id={`photo-${i}`}>
                <a href="#close" class="lightbox__bg" aria-label="Close"></a>
                <div class="lightbox__inner">
                  <img src={p.src} alt={p.alt} />
                  <p class="lightbox__cap">{p.caption}</p>
                  <a class="lightbox__x" href="#close" aria-label="Close">×</a>
                </div>
              </div>
            </figure>
          ))}
        </div>

        <p class="muted note">Got a favorite trail memory or access note to share? Drop it in <a href="/trail-notes">Trail Notes</a>.</p>
      </div>
    </section>

    <!-- Closing -->
    <section class="cta">
      <div class="wrap cta__inner">
        <h2>Ready to plan your next wander?</h2>
        <p>Free campsites, real access notes, and print‑ready detail pages — built by hikers, shared by everyone.</p>
        <div class="btnrow">
          <a class="btn btn--primary" href="/explore">Explore the Map</a>
          <a class="btn btn--outline" href="/about-mission">Back to Mission</a>
        </div>
      </div>
    </section>
  </main>

  <style>
    :root{
      --ink:#E87722;        /* tribal orange */
      --paper:#121212;      /* deep background */
      --paper-2:#0e0e0e;
      --text:#eaeaea;
    }
    .scrapbook{ background:var(--paper); color:var(--text); }

    .wrap{ max-width:1100px; margin:0 auto; padding:0 1.1rem; }

    /* Hero */
    .hero{ position:relative; overflow:hidden; padding:4.5rem 1rem 2.2rem; border-bottom:1px solid #141414; }
    .hero__paper{ position:absolute; inset:0; background:
      radial-gradient(70% 60% at 50% 0%, rgba(255,165,60,.08), transparent 60%),
      url('/images/hero/topo-dark5.png') center/cover no-repeat; opacity:.35; }
    .hero__ink{ position:absolute; inset:0; background:
      linear-gradient(to bottom, rgba(0,0,0,.55), transparent 30%, rgba(0,0,0,.6)); }
    .hero__inner{ position:relative; z-index:1; text-align:center; }
    .title{ font-family:'Bebas Neue',sans-serif; text-transform:uppercase; letter-spacing:.04em;
      font-size:clamp(2.2rem,6vw,3.8rem); margin:0 0 .4rem;
      background:linear-gradient(45deg,#ffd24d,#cc9900); -webkit-background-clip:text; -webkit-text-fill-color:transparent;
      text-shadow:0 2px 10px rgba(0,0,0,.6);
    }
    .lede{ color:#f6e3c8; max-width:820px; margin:0 auto 1rem; }
    .hero__icons{ display:flex; gap:.6rem; justify-content:center; opacity:.85; }
    .hero__icons img{ width:42px; height:42px; image-rendering:auto; }

    /* Paper card */
    .story{ background:var(--paper-2); padding:2.2rem 0; }
    .paper-card{ background:#141414; border:1px solid #1d1d1d; border-radius:16px; padding:1.1rem 1.2rem;
      box-shadow:0 10px 26px rgba(0,0,0,.28); }
    .paper-card h2{ color:var(--ink); font-family:'Bebas Neue',sans-serif; letter-spacing:.03em; margin:.2rem 0 .4rem; }

    /* Mini features */
    .mini{ background:#101010; padding:1.4rem 0 1.8rem; }
    .mini__grid{ display:grid; gap:1rem; grid-template-columns:repeat(auto-fit,minmax(240px,1fr)); }
    .mini__card{ background:#141414; border:1px solid #212121; border-radius:16px; padding:1rem; }
    .mini__card h3{ color:var(--ink); font-family:'Bebas Neue',sans-serif; margin:.1rem 0 .3rem; }

    /* Gallery */
    .gallery{ padding:2.2rem 0 2.8rem; background:#0f0f0f; }
    .subhead{ text-align:center; color:var(--ink); font-family:'Bebas Neue',sans-serif; letter-spacing:.03em; margin:0 0 1rem; }
    .grid{ columns: 1; column-gap: 1rem; }
    @media (min-width: 700px){ .grid{ columns: 2; } }
    @media (min-width: 1024px){ .grid{ columns: 3; } }

    .polaroid{ break-inside: avoid; display:block; margin:0 0 1rem; position:relative; transform: rotate(var(--r,0deg)); }
    .polaroid img{ width:100%; height:auto; display:block; border-radius:12px; box-shadow:0 14px 36px rgba(0,0,0,.32); }
    .polaroid figcaption{ font-family: ui-rounded, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial;
      font-size:.95rem; color:#f0e9dc; background:#191919; border:1px solid #232323; border-top:none;
      border-radius:0 0 12px 12px; padding:.55rem .7rem; }
    /* masking tape corners */
    .polaroid::before,
    .polaroid::after{
      content:""; position:absolute; width:62px; height:18px; background:rgba(255,214,138,.8);
      top:-8px; left:14px; transform: rotate(-4deg); border-radius:2px;
      box-shadow:0 2px 5px rgba(0,0,0,.25);
    }
    .polaroid::after{ left:auto; right:14px; transform: rotate(3deg); }

    /* Lightbox (pure CSS via :target) */
    .lightbox{ position:fixed; inset:0; display:none; z-index:80; }
    .lightbox:target{ display:block; }
    .lightbox__bg{ position:absolute; inset:0; background:rgba(0,0,0,.82); backdrop-filter:saturate(1.2) blur(2px); }
    .lightbox__inner{ position:absolute; inset:0; display:grid; place-items:center; padding:1.2rem; }
    .lightbox__inner img{ max-width:min(92vw,1200px); max-height:82vh; border-radius:12px; box-shadow:0 18px 48px rgba(0,0,0,.5); }
    .lightbox__cap{ color:#f2eadc; margin:.6rem 0 0; text-align:center; }
    .lightbox__x{ position:absolute; top:10px; right:16px; font-size:2rem; color:#fff; text-decoration:none; }

    .note{ text-align:center; color:#cfcfcf; margin:1rem 0 0; }
    .note a{ color:var(--ink); }

    /* CTA */
    .cta{ background:#0d0d0d; border-top:1px solid #161616; }
    .cta__inner{ text-align:center; padding:2rem 0 2.6rem; }
    .cta__inner h2{ font-family:'Bebas Neue',sans-serif; color:var(--ink); letter-spacing:.03em; margin:.2rem 0 .5rem; }
    .btnrow{ display:flex; gap:.8rem; justify-content:center; flex-wrap:wrap; }
    .btn{ display:inline-flex; align-items:center; justify-content:center; padding:.9rem 1.3rem; border-radius:12px; font-weight:800; text-decoration:none; transition:.15s ease transform, .15s ease box-shadow; }
    .btn:hover{ transform: translateY(-1px); }
    .btn--primary{ background:var(--ink); color:#0c0c0c; box-shadow:0 10px 28px rgba(232,119,34,.28); }
    .btn--outline{ border:2px solid var(--ink); color:var(--ink); }
  </style>
</BaseLayout>
